---
title: "browser_wait"
description: "Wait until text appears/disappears, the URL contains a string, or a ref reaches a state."
---

{/* Generated by `bun run docs:tools` from companion/src. Edit descriptions in the source, examples in companion/test/docs-examples.ts. */}

Wait until text appears/disappears, the URL contains a string, or a ref reaches a state. Polls up to timeoutMs (default 15000).

<Note>The tool names stay the same across browsers. See [browser selection](/reference/multiple-browsers) and [Firefox and Zen exceptions](/reference/firefox) before choosing an operation.</Note>


## Parameters

<ParamField body="tabId" type="integer">
  Target tab id from browser_tabs, which lists tabs across all windows. Defaults to the agent's most recently opened usable tab, or the only usable tab. Otherwise select a listed tabId.
</ParamField>

<ParamField body="text" type="string">
  &nbsp;
</ParamField>

<ParamField body="textGone" type="string">
  &nbsp;
</ParamField>

<ParamField body="url" type="string">
  &nbsp;
</ParamField>

<ParamField body="ref" type="string">
  Element ref from browser_snapshot, e.g. "e12"
</ParamField>

<ParamField body="state" type="enum">
  One of: `visible`, `hidden`, `enabled`.
</ParamField>

<ParamField body="timeoutMs" type="integer">
  Range: … to 120000.
</ParamField>

## Example

```json 
{
  "tabId": 1234,
  "text": "Order confirmed",
  "timeoutMs": 20000
}
```

```json Until the URL changes
{
  "tabId": 1234,
  "url": "/dashboard"
}
```

```json Until a ref is enabled
{
  "tabId": 1234,
  "ref": "e3",
  "state": "enabled"
}
```
