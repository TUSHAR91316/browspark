---
title: "browser_fetch"
description: "Fetch a URL and return its main content as markdown (or text/html) without leaving a tab behind: opens a background agent tab, waits for load, reads, closes."
---

{/* Generated by `bun run docs:tools` from companion/src. Edit descriptions in the source, examples in companion/test/docs-examples.ts. */}

Fetch a URL and return its main content as markdown (or text/html) without leaving a tab behind: opens a background agent tab, waits for load, reads, closes. Use for reading docs and articles.


## Parameters

<ParamField body="url" type="string" required>
  &nbsp;
</ParamField>

<ParamField body="what" type="enum" required default="markdown">
  One of: `markdown`, `text`, `html`.
</ParamField>

<ParamField body="context" type="string">
  &nbsp;
</ParamField>

<ParamField body="keepTab" type="boolean">
  &nbsp;
</ParamField>

<ParamField body="timeoutMs" type="integer">
  &nbsp;
</ParamField>

## Example

```json
{
  "url": "https://developer.chrome.com/docs/devtools/",
  "what": "markdown"
}
```
