---
title: "browser_session"
description: "Developer-mode browsers: a separate Chromium or Firefox browser the companion launches, without the user's logins."
---

{/* Generated by `bun run docs:tools` from companion/src. Edit descriptions in the source, examples in companion/test/docs-examples.ts. */}

Developer-mode browsers: a separate Chromium or Firefox browser the companion launches, without the user's logins. Only launch one when the user explicitly asks for it (pass userRequested: true, never on your own initiative), or when a tool reported that an operation needs developer mode (heap snapshots, Lighthouse, raw CDP). For everyday work use the user's shared tabs. Each named context has its own persistent profile. Firefox uses WebDriver BiDi and has documented exceptions for advanced tools; shared-tab extension mode remains Chromium-only; several can run in parallel. launch/close/status take a context (default "default"); contexts lists saved profiles; delete removes a stopped context's profile.

<Note>Everyday work happens in the user's shared tabs. The dashboard's Settings page decides whether a separate browser may open at all (Only when needed, Always, Never).</Note>

## Parameters

<ParamField body="action" type="enum" required>
  One of: `launch`, `status`, `close`, `contexts`, `delete`.
</ParamField>

<ParamField body="context" type="string">
  Context name, e.g. "work" or "personal" (default "default")
</ParamField>

<ParamField body="all" type="boolean">
  close: close every running context
</ParamField>

<ParamField body="url" type="string">
  Initial URL for launch
</ParamField>

<ParamField body="userRequested" type="boolean">
  launch: the user explicitly asked for a developer browser in their own words. Satisfies the "Only when needed" dashboard setting. Never set it on your own initiative.
</ParamField>

<ParamField body="headless" type="boolean">
  Launch without a window; screenshots work in either browser, live view is Chromium-only
</ParamField>

<ParamField body="browser" type="enum">
  launch/delete: browser engine (default chromium for a new context). Firefox uses a separate profile and WebDriver BiDi. One of: `chromium`, `firefox`.
</ParamField>

<ParamField body="chromePath" type="string">
  &nbsp;
</ParamField>

<ParamField body="firefoxPath" type="string">
  Firefox, LibreWolf, or Zen executable; alternatively BROWSPARK_FIREFOX
</ParamField>

<ParamField body="args" type="string[]">
  Extra browser switches
</ParamField>

<ParamField body="devtools" type="boolean">
  Chromium only: open DevTools for every tab (default true unless headless); Firefox rejects true
</ParamField>

<ParamField body="proxy" type="string">
  Proxy server for this browser, e.g. "http://proxy:8080" or "socks5://127.0.0.1:1080"
</ParamField>

<ParamField body="extensions" type="string[]">
  Chromium only: unpacked extension directories to load
</ParamField>

<ParamField body="downloadDir" type="string">
  Where downloads land (default ~/.browspark/downloads/&lt;context&gt; for Chromium, ~/.browspark/downloads/firefox/&lt;context&gt; for Firefox)
</ParamField>

## Example

```json Launch the default context
{
  "action": "launch"
}
```

```json Launch because the user asked for a developer browser
{
  "action": "launch",
  "userRequested": true
}
```

```json Launch Firefox in an isolated profile
{
  "action": "launch",
  "browser": "firefox",
  "context": "firefox",
  "userRequested": true
}
```

```json Launch a named, headless context behind a proxy
{
  "action": "launch",
  "context": "scraper",
  "headless": true,
  "proxy": "socks5://127.0.0.1:1080",
  "url": "https://example.com"
}
```

```json Close everything
{
  "action": "close",
  "all": true
}
```
