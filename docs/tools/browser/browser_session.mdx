---
title: "browser_session"
description: "Developer-mode browsers: launch Chrome, Brave, Firefox, or Zen without the user's logins."
---

{/* Generated by `bun run docs:tools` from companion/src. Edit descriptions in the source, examples in companion/test/docs-examples.ts. */}

Developer-mode browsers: launch Chrome, Brave, Firefox, or Zen without the user's logins. Only launch when the user explicitly asks (pass userRequested: true, never on your own initiative), or a tool reported it needs developer mode. For everyday work use shared tabs. Each named context has its own persistent profile; use different context names to run browsers simultaneously. chromium preserves automatic Chrome/Chromium discovery. Firefox and Zen use WebDriver BiDi with documented exceptions; extension sharing remains Chromium-only. launch/close take a context (default "default"); status lists running browsers; contexts lists saved profiles; delete removes a stopped profile.

<Note>The tool names stay the same across browsers. See [browser selection](/reference/multiple-browsers) and [Firefox and Zen exceptions](/reference/firefox) before choosing an operation.</Note>

<Note>Use a different context name for each running browser. Connected dashboards apply the most restrictive developer-browser setting: Never, then Only when needed, then Always. An explicit user request satisfies Only when needed.</Note>

## Parameters

<ParamField body="action" type="enum" required>
  One of: `launch`, `status`, `close`, `contexts`, `delete`.
</ParamField>

<ParamField body="context" type="string">
  Context name, e.g. "work" or "personal" (default "default")
</ParamField>

<ParamField body="all" type="boolean">
  close: close every running context
</ParamField>

<ParamField body="url" type="string">
  Initial URL for launch
</ParamField>

<ParamField body="userRequested" type="boolean">
  launch: the user explicitly asked for a developer browser in their own words. Satisfies the "Only when needed" dashboard setting. Never set it on your own initiative.
</ParamField>

<ParamField body="headless" type="boolean">
  Launch without a window; screenshots work in either browser, live view is Chromium-only
</ParamField>

<ParamField body="browser" type="enum">
  launch/delete: browser brand (default chromium for a new context). One of: `chromium`, `chrome`, `brave`, `firefox`, `zen`.
</ParamField>

<ParamField body="browserPath" type="string">
  Executable for the selected browser; overrides automatic discovery.
</ParamField>

<ParamField body="chromePath" type="string">
  Legacy executable override for Chromium browsers
</ParamField>

<ParamField body="firefoxPath" type="string">
  Legacy executable override for Firefox browsers
</ParamField>

<ParamField body="args" type="string[]">
  Extra browser switches
</ParamField>

<ParamField body="devtools" type="boolean">
  Chromium only: open DevTools for every tab (default true unless headless); Firefox rejects true
</ParamField>

<ParamField body="proxy" type="string">
  Proxy server for this browser, e.g. "http://proxy:8080" or "socks5://127.0.0.1:1080"
</ParamField>

<ParamField body="extensions" type="string[]">
  Chromium only: unpacked extension directories to load
</ParamField>

<ParamField body="downloadDir" type="string">
  Where downloads land (default ~/.browspark/downloads/&lt;browser&gt;/&lt;context&gt;; legacy chromium omits the browser directory)
</ParamField>

## Example

```json Launch the default context
{
  "action": "launch"
}
```

```json Launch because the user asked for a developer browser
{
  "action": "launch",
  "userRequested": true
}
```

```json Launch Firefox in an isolated profile
{
  "action": "launch",
  "browser": "firefox",
  "context": "firefox",
  "userRequested": true
}
```

```json Launch Chrome in a named context
{
  "action": "launch",
  "browser": "chrome",
  "context": "chrome-work",
  "userRequested": true
}
```

```json Run Brave alongside the other contexts
{
  "action": "launch",
  "browser": "brave",
  "context": "brave-work",
  "userRequested": true
}
```

```json Run Zen in its own profile
{
  "action": "launch",
  "browser": "zen",
  "context": "zen-work",
  "userRequested": true
}
```

```json Launch a named, headless context behind a proxy
{
  "action": "launch",
  "context": "scraper",
  "headless": true,
  "proxy": "socks5://127.0.0.1:1080",
  "url": "https://example.com"
}
```

```json Close everything
{
  "action": "close",
  "all": true
}
```
