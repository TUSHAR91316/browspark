---
title: "devtools_memory"
description: "Memory panel."
---

{/* Generated by `bun run docs:tools` from companion/src. Edit descriptions in the source, examples in companion/test/docs-examples.ts. */}

Memory panel. snapshot captures a .heapsnapshot artifact (opens in DevTools Memory) and summarizes objects by class with sizes and detached DOM node count. compare diffs two snapshots by class (growth). retainers lists what holds instances of a class. sampling start/stop records an allocation sampling profile (.heapprofile) with top allocating functions. usage returns current heap size; growth samples heap usage over time to spot leaks.

<Note>The tool names stay the same across browsers. See [browser selection](/reference/multiple-browsers) and [Firefox and Zen exceptions](/reference/firefox) before choosing an operation.</Note>

<Note>Heap snapshots, class diffs, retainers and allocation sampling need Chromium developer mode because extensions cannot use HeapProfiler. usage and growth also work in Chromium extension mode. Firefox and Zen do not support this tool.</Note>

## Parameters

<ParamField body="tabId" type="integer">
  Target tab id from browser_tabs, which lists tabs across all windows. Defaults to the agent's most recently opened usable tab, or the only usable tab. Otherwise select a listed tabId.
</ParamField>

<ParamField body="action" type="enum" required>
  One of: `snapshot`, `compare`, `retainers`, `sampling`, `usage`, `growth`, `list`.
</ParamField>

<ParamField body="snapshotId" type="string">
  &nbsp;
</ParamField>

<ParamField body="other" type="string">
  &nbsp;
</ParamField>

<ParamField body="className" type="string">
  &nbsp;
</ParamField>

<ParamField body="limit" type="integer">
  &nbsp;
</ParamField>

<ParamField body="phase" type="enum">
  For sampling One of: `start`, `stop`.
</ParamField>

<ParamField body="intervalBytes" type="integer">
  &nbsp;
</ParamField>

<ParamField body="seconds" type="number">
  growth: duration, default 10
</ParamField>

<ParamField body="gc" type="boolean">
  Collect garbage first (default true for snapshot)
</ParamField>

## Example

```json 
{
  "tabId": 5001,
  "action": "snapshot"
}
```

```json Growth between two snapshots
{
  "tabId": 5001,
  "action": "compare",
  "snapshotId": "snap-2",
  "other": "snap-1"
}
```

```json Sample heap usage for 20 seconds
{
  "tabId": 1234,
  "action": "growth",
  "seconds": 20,
  "gc": true
}
```
