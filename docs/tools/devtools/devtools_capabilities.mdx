---
title: "devtools_capabilities"
description: "Report the browser version, connection mode, and which CDP domains work for a tab (probed live, cached per browser)."
---

{/* Generated by `bun run docs:tools` from companion/src. Edit descriptions in the source, examples in companion/test/docs-examples.ts. */}

Report the browser version, connection mode, and which CDP domains work for a tab (probed live, cached per browser). Extension mode blocks some domains that direct CDP allows.


## Parameters

<ParamField body="tabId" type="integer">
  Target tab id from browser_tabs, which lists tabs across all windows. Defaults to the agent's most recently opened usable tab, or the only usable tab. Otherwise select a listed tabId.
</ParamField>

<ParamField body="refresh" type="boolean">
  &nbsp;
</ParamField>

## Example

```json
{
  "tabId": 1234
}
```
